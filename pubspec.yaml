name: coolmovies
description: A cooll movies project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  graphql_flutter: ^5.1.2
  equatable: ^2.0.5
  cupertino_icons: ^1.0.2
  provider: ^6.1.2
  go_router: ^13.2.1
  bloc: ^8.1.4
  flutter_bloc: ^8.1.5

  movies_repository:
    path: packages/movies_repository

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
